#include "main.h"
/**
 * _isupper - uppercase letters
 * @c: parameter
 *
 * Return: result (success)
 */

int _isupper(int c)
{
        	if (c >= 'A' && c <= 'Z')
                    	return (1);
        	else
        	return (0);
}
