#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @n: parameter
 * Return: computed value (success)
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
